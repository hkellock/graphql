{
  "scripts": {
    "prettier": "cd api && yarn prettier && cd ../web && yarn prettier && cd ../next && yarn prettier",
    "format": "cd api && yarn format && cd ../web && yarn format && cd ../next && yarn format"
  }
}